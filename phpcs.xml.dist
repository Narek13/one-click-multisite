<?xml version="1.0"?>
<ruleset name="One Click Multisite">
    <description>WordPress Coding Standards for One Click Multisite plugin.</description>

    <arg name="colors"/>
    <arg value="sp"/>
    <arg name="extensions" value="php"/>

    <file>./src</file>
    <file>./one-click-multisite.php</file>
    <file>./tests</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>

    <!-- WordPress ruleset -->
    <rule ref="WordPress"/>
    <rule ref="WordPress-Extra"/>
    <rule ref="WordPress-Docs"/>

    <!-- Enforce that all global functions/variables/constants are prefixed -->
    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array">
                <element value="one_click_multisite"/>
                <element value="OneClickMultisite"/>
            </property>
        </properties>
    </rule>

    <!-- Enforce text domain -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array">
                <element value="one-click-multisite"/>
            </property>
        </properties>
    </rule>

    <!-- PHP version compatibility -->
    <config name="testVersion" value="7.4-"/>
    <config name="minimum_supported_wp_version" value="6.0"/>

    <!-- The plugin uses PascalCase class names inside namespaces (e.g. AdminModule).
         WordPress.Files.FileName.InvalidClassFileName expects the filename to mirror the
         class name lowercased without hyphens (class-adminmodule.php), which conflicts
         with the conventional hyphenated form (class-admin-module.php). Since the files
         already satisfy NotHyphenatedLowercase, exclude the class-name-match rule. -->
    <rule ref="WordPress.Files.FileName.InvalidClassFileName">
        <exclude-pattern>src/*</exclude-pattern>
    </rule>

    <!-- Tests use PHPUnit naming (*Test.php, AbstractTestCase.php) which conflicts
         with WordPress file naming conventions. Exclude file name checks for tests. -->
    <rule ref="WordPress.Files.FileName">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

    <!-- PHPUnit lifecycle methods (setUp, tearDown) and test method names follow
         PHPUnit conventions and cannot be renamed to snake_case. -->
    <rule ref="WordPress.NamingConventions.ValidFunctionName">
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>

    <!-- Type hints and strict declarations are intentional -->
    <rule ref="Squiz.Commenting.FunctionComment.MissingParamComment">
        <severity>0</severity>
    </rule>
</ruleset>
